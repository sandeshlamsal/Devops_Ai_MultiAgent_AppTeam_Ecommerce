REDIS_URL=redis://localhost:6379
LOG_LEVEL=info

# No ANTHROPIC_API_KEY needed.
# Agents call the local `claude` CLI which uses your existing Claude Code
# authentication (~/.claude). Make sure `claude` works in your terminal first.
